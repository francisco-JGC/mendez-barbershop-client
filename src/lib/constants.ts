export const BARBERSHOP_DISPLAY_NAME = 'Mendez Barbershop';

// Only one establishment exists for now, so the login form doesn't ask for
// a tenant code — it's sent automatically. Update this if the barbershop's
// code in the backend ever changes, or revisit this once there's more than
// one tenant.
export const DEFAULT_TENANT_CODE = 'mendez';
